# Folder Manager (fm) - pathlib-version

OS. 함수 대신 pathlib을 사용한 버전으로 코드가 20줄이상 길어지고 이런 간단한 스크립트에서 오버엔지니어링이 될 수 있다고 판단하여 별도의 브랜치로 분리 되었습니다.

The version using pathlib instead of OS. functions was over 20 lines long and could be considered over-engineering for such a simple script, so it was split into a separate branch.
